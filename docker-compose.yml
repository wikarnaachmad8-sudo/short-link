services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: shortlink-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8098:8098"

    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true

